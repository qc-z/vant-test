export function getPackNames() {
  // console.log(import.meta.env)
  // const modules = import.meta.globEager('../utils/index.js')
  // const packages = []
  // Object.keys(modules).forEach((key) => {
  //   const name = key.split('/').slice(-1)[0].split('.')[0]
  //   const mod = modules[key].default || {}
  //   console.log(mod, '222')
  //   packages.push(mod)
  // })
  // return {
  //   packages
  // }
}